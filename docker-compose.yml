services:
  sqlserver:
    platform: linux/amd64
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: videogames-sqlserver
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=0BEG9se8BAoNtR0umpHR
      - MSSQL_PID=Express
    ports:
      - "1433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    networks:
      - videogames-network

  backend:
    build:
      context: .
      dockerfile: VideoGamesCatalogue.Server/Dockerfile
    container_name: videogames-backend
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=https://+:443;http://+:80
      - ASPNETCORE_Kestrel__Certificates__Default__Path=/https-dev-cert.pfx
      - ASPNETCORE_Kestrel__Certificates__Default__Password=Password123!
    ports:
      - "55027:443"
    depends_on:
      - sqlserver
    networks:
      - videogames-network

  frontend:
    build:
      context: ./videogamescatalogue.client
      dockerfile: Dockerfile
    container_name: videogames-frontend
    environment:
      - VITE_API_URL=https://localhost:55027/api
    ports:
      - "55028:5173"
    depends_on:
      - backend
    networks:
      - videogames-network

volumes:
  sqlserver_data:
    driver: local

networks:
  videogames-network:
    driver: bridge